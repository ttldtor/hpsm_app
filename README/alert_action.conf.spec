[hpsm]

param.url = <string>
* HPSM REST API URL

param.login = <string>
* HPSM user login

param.password = <string>
* HPSM user password

param.category = <string>
* HPSM incident category (default: incident)

param.from = <string>
* Splunk service name (default: splunk)